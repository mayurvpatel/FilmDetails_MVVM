object App {
    const val applicationId = "com.task.practical"

    object Dimension {
        const val DEFAULT = "default"
        const val ENVIRONMENT = "environment"
    }

    object Type {
        const val debugType = "debug"
        const val qaType = "qa"
        const val releaseType = "release"
    }
}
